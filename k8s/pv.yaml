apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/xxxx/mysql-data"  # Ubuntuホスト上の安全な保存先 xxxはユーザー名
    type: DirectoryOrCreate